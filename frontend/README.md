# Frontend environment

Create a local environment file with:

- VITE_API_BASE_URL=http://localhost:8000/api

The app falls back to demo data when the backend is unavailable, so the first run can still be explored without a live API.
