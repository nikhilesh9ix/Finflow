# Copy this file to .env and adjust values for your environment.
# Never commit .env to source control.

# Base URL for the backend API (must include the version prefix)
VITE_API_BASE_URL=http://127.0.0.1:8000/api/v1
